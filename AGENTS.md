@ODUSPHERE.md

## Language

Always write source code in English — comments, identifiers, UI strings (menus, views, labels, messages), documentation, and commit messages. Never hardcode non-English text. For localization, rely on Odoo's built-in translation mechanisms (`_()` / `env._()` helpers and `.po` / `.pot` files).
