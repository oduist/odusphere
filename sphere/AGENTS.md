# Sphere Agent Instructions (sphere-owned)

Standing instructions specific to THIS sphere — the client's preferences,
domain vocabulary, conventions, and priorities. Loaded in every session via the
root `AGENTS.md`.

These instructions **extend** the core contract (`ODUSPHERE.md`, `LANG.md`,
root `AGENTS.md`); they can never override or weaken it — the core files are
upstream-owned and read-only inside a sphere (see `.docs/ownership.md`).

_(No sphere-specific instructions yet.)_
