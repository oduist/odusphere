# 2026-07-01

> Context: the root language config was split into an upstream-owned **policy**
> (`LANG.md`) and a sphere-owned **selection** (`LANG.local.md`), mirroring the
> `.docs/architecture.md` / `.docs/architecture.local.md` split. `LANG.md` left
> the `merge=ours` lane (upstream policy now flows in); `LANG.local.md` took its
> place as the sphere-owned file. The split itself (`LANG.md`/`LANG.local.md`,
> `.gitattributes`, `AGENTS.md`, the `odu-doc-i18n` skill) is repository
> infrastructure, not part of the odu_book module SPEC — only the deltas below
> touch odu_book's own docs.

## admin_guide.md

### Changed

- In **"Upstream updates — protected core modules"**, the example list of
  sphere-owned files the update preserves now cites `LANG.local.md` instead of
  `LANG.md` — the policy file `LANG.md` is upstream-owned and taken from upstream
  on update; only the sphere's selection file `LANG.local.md` is kept.

```diff
- the update keeps this sphere's `website/`, its own `odu_*` modules, `README.md`,
- `LANG.md` and the sphere system map `.docs/architecture.local.md`.)
+ the update keeps this sphere's `website/`, its own `odu_*` modules, `README.md`,
+ `LANG.local.md` and the sphere system map `.docs/architecture.local.md`.)
```

- In **"Documentation languages"**, the set of languages is now described as
  defined in `LANG.local.md` (with the rules for those fields in the
  upstream-owned `LANG.md`).

```diff
- The set of languages is defined in the project's `LANG.md` file. Today the
- documentation ships in English only (`source: en`, no targets yet).
+ The set of languages is defined in the project's `LANG.local.md` file (the
+ rules for those fields live in the upstream-owned `LANG.md`). Today the
+ documentation ships in English only (`source: en`, no targets yet).
```

## tech_spec.md

### Changed

- In the **Multilingual read-path** rules, the runtime-independence note now
  names both files: the read-path depends on neither `LANG.md` nor
  `LANG.local.md`; authoring is governed by `LANG.md` (policy) + `LANG.local.md`
  (selections).

```diff
- There is no `LANG.md` dependency at runtime — the rule is purely
- "translated-if-present, else source". `LANG.md` governs authoring
- (the `odu-doc-i18n` skill), not serving.
+ There is no `LANG.md` / `LANG.local.md` dependency at runtime — the rule is
+ purely "translated-if-present, else source". `LANG.md` (policy) +
+ `LANG.local.md` (selections) govern authoring (the `odu-doc-i18n` skill),
+ not serving.
```
